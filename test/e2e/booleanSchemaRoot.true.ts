// A document that is nothing but `true` (draft 6+) admits every value. It has no keyword to be
// named by, so it is declared under the file's name -- where it used to yield an empty file
export const input = true
