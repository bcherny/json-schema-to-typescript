{
  "title": "Baz",
  "type": "object",
  
  "properties": {
  
    "firstName": {
      "type": "string"
    },
    
    "age": {
      "type": "integer",
      "minimum": 0
    },

    "bar": {
      "required": true,
      "$ref": "bar.json"
    }
  },
  
  "required": ["firstName", "lastName"]
}